---
name: api-designer
description: Delegate to the API Designer agent. Use for REST API design, gRPC schema design, GraphQL schema design, OpenAPI specifications, and API versioning strategy.
---

# Skill: API Designer

Delegate this request to the `api-designer` specialist via the Agent tool.

## Instructions

Use the Agent tool with `subagent_type: api-designer`. Pass the full user request as the prompt, including relevant domain context, consumer requirements, and any existing API contracts.

Prompt to pass: $ARGUMENTS
