---
name: bash-agent
description: Delegate Bash scripting tasks to the Bash specialist agent. Use for writing, reviewing, or hardening shell scripts with safe defaults, portability, and static analysis (shellcheck).
---

# Skill: Bash Agent

Delegate this request to the `bash-agent` specialist via the Agent tool.

## Instructions

Use the Agent tool with `subagent_type: bash-agent`. Pass the full user request as the prompt, including any relevant file context or constraints.

Prompt to pass: $ARGUMENTS
