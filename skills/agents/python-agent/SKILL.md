---
name: python-agent
description: Delegate Python coding tasks to the Python specialist agent. Use for writing, reviewing, or refactoring Python 3.12+ code with modern idioms, type hints, and tooling (uv, ruff, mypy).
---

# Skill: Python Agent

Delegate this request to the `python-agent` specialist via the Agent tool.

## Instructions

Use the Agent tool with `subagent_type: python-agent`. Pass the full user request as the prompt, including any relevant file context or constraints.

Prompt to pass: $ARGUMENTS
