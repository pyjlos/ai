---
name: kubernetes
description: Delegate to the Kubernetes specialist agent. Use for Kubernetes workload design, manifest authoring, Helm chart development, resource sizing, and cluster operations.
---

# Skill: Kubernetes Agent

Delegate this request to the `kubernetes` specialist via the Agent tool.

## Instructions

Use the Agent tool with `subagent_type: kubernetes`. Pass the full user request as the prompt, including relevant cluster version, workload type, and operational requirements.

Prompt to pass: $ARGUMENTS
