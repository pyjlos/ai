---
name: typescript-agent
description: Delegate TypeScript coding tasks to the TypeScript specialist agent. Use for writing, reviewing, or refactoring type-safe TypeScript 5.x code with strict mode, modern patterns, and ecosystem tooling.
---

# Skill: TypeScript Agent

Delegate this request to the `typescript-agent` specialist via the Agent tool.

## Instructions

Use the Agent tool with `subagent_type: typescript-agent`. Pass the full user request as the prompt, including any relevant file context or constraints.

Prompt to pass: $ARGUMENTS
