---
name: sql-agent
description: Delegate SQL tasks to the SQL specialist agent. Use for query writing, schema design, performance optimization, migrations, and database best practices with a PostgreSQL primary focus.
---

# Skill: SQL Agent

Delegate this request to the `sql-agent` specialist via the Agent tool.

## Instructions

Use the Agent tool with `subagent_type: sql-agent`. Pass the full user request as the prompt, including any relevant schema, query, or constraint context.

Prompt to pass: $ARGUMENTS
