---
name: docker
description: Delegate to the Docker specialist agent. Use for Dockerfile authoring, image optimization, multi-stage builds, container security, and Docker Compose configuration.
---

# Skill: Docker Agent

Delegate this request to the `docker` specialist via the Agent tool.

## Instructions

Use the Agent tool with `subagent_type: docker`. Pass the full user request as the prompt, including relevant language/runtime, base image preferences, and deployment target context.

Prompt to pass: $ARGUMENTS
